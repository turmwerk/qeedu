import React from "react";
import { WorkspaceEntryRedirect } from "@/feature/RecordWorkspace";

const SyllabusEntry: React.FC = () => {
  return <WorkspaceEntryRedirect to="/teaching/syllabus" />;
};

export default SyllabusEntry;
