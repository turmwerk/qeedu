import React from "react";
import { WorkspaceEntryRedirect } from "@/feature/RecordWorkspace";

const ExamDesignEntry: React.FC = () => {
  return <WorkspaceEntryRedirect to="/teaching/exam" />;
};

export default ExamDesignEntry;
